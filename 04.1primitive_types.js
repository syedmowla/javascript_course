/* Primitive data types can only hold one data type for example string, number, boolean, 
BigInt, undefined, null, symbol and object 
There are fundamental values */
/* Initialise a string data type in three ways, with "", '', and `` */

let stringOne = "stringOne";
let stringTwo = 'stringTwo';
let stringThree = `stringThree`;

console.log(stringOne);
console.log(stringTwo);
console.log(stringThree);

/* Undefined variable type will just return the term undefined. This is when 
we initialise a variable but we do not define it */

let undefinedVariable;
console.log(undefinedVariable); /* returns undefined */

/* null allows you to essentially clear the value of a variable */